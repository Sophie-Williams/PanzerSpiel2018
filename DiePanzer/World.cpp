#include "World.h"

void World::Apply_moves(uint_fast32_t tick) {
	GameObject* currentObject = objects.begin;
	Tank* currentTank;

	for each (GameObject currentObject in objects) {
		if (currentObject->Get_type() == ClassID::tank_standart) {
			Tank = GameObject;
			
		}
	}
}